en:
  site_settings:
    location_input_fields: "Location input fields"
    location_input_fields_enabled: "Enable location input fields"
    location_geocoding: "Whether locations should be geocoded (mapped). If off, locations are stored as strings."
    location_geocoding_api_key: "Geocoding API key"
    location_geocoding_provider: "Geocoding provider (<a href='https://github.com/alexreisner/geocoder#street-address-services' target='_blank'>provider details</a>). If the provider
                                  requires an api key enter it below before selecting the provider."
    location_geocoding_timeout: "Geocoding timeout"
    location_map_tile_layer: "Map tile layer uri (<a href='https://leaflet-extras.github.io/leaflet-providers/preview/'>examples</a>)"
    location_map_tile_layer_subdomains: "Map tile layer subdomains"
    location_map_attribution: "Map attribution"

  location:
    errors:
      no_api_key: "No api key set for that provider."
