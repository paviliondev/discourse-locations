<div class="location-label" title="{{i18n 'location.label.title'}}">
  {{d-icon 'map-marker'}}
  {{location-format topic.location}}
</div>
{{#if hasGeoLocation}}
  <div class="location-topic-map">
    <button {{action "showMap"}} class='btn btn-default'>
      {{d-icon 'map-o'}}
      {{#unless site.mobileView}}
        {{i18n 'add_to_calendar.label'}}
      {{/unless}}
    </button>
    {{#if showMap}}
      {{map-component topic=topic}}
    {{/if}}
  </div>
{{/if}}
